# BMAD commands

This directory contains repository-specific command helpers for BMAD workflows.

Available (placeholder) commands:

- `init` — initialize BMAD (created by bmad-master)
- `status` — show workflow status

You can implement GitHub Commands or scripts here to automate the BMAD workflows.
