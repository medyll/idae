# idae-mono-expand-vitrine

CLI to sync pnpm monorepo packages into individual GitHub showcase repositories.

Install dependencies at the repo root, then run:

```
node ./packages/idae-mono-expand-vitrine/bin/sync-vitrine.js --dry-run --verbose
```
