# S26-03 — Update toutes les références _prefs/_activity/_history → appuser_*

**Sprint:** 26
**Effort:** M
**Priority:** high
**Dependencies:** S26-02

---

## Goal

Après le rename dans idae-model-core, trouver et corriger toutes les références aux anciens noms dans `src/` et `server/`.

## Search command

```bash
grep -rn "_prefs\|_activity\|_history" src/ server/ --include="*.ts" --include="*.svelte"
```

## Known locations à vérifier

- `src/lib/main/machine.ts` — `buildEffectiveModel()` si référence directe
- `src/lib/main/__tests__/machinePrefs.test.ts` — string literals `'_prefs'`
- `src/lib/main/__tests__/machineActivity.test.ts` — string literals `'_activity'`
- `src/lib/main/__tests__/machineHistory.test.ts` — string literals `'_history'`
- Tout autre test ou composant qui accède `machine.store['_prefs']` etc.

## Acceptance

- Grep `'_prefs'\|'_activity'\|'_history'` → 0 résultats dans `src/` et `server/` (hors commentaires et bmad/)
- `pnpm run check` → 0 new errors
