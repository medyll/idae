# Story S8-01 — GET /api/scheme → IdbqModel JSON

**Sprint:** 8  
**Goal:** Serve IdbqModel-compatible JSON from appscheme_* collections

## Implemented

| File | Action |
|------|--------|
| `server/src/routes/scheme.ts` | REWRITE — reads appscheme + appscheme_field from {org}_machine_app |
| `server/src/config.ts` | MOD — add `org` field (ORG env var, default 'test') |

## GET /api/scheme response shape

```json
{
  "vehicle": {
    "keyPath": "++id",
    "base": "machine_base",
    "model": {},
    "template": {
      "index": "id",
      "presentation": "license_plate model brand year status",
      "fields": { "id": { "type": "id", "required": false, "readonly": true, "private": false }, ... },
      "fks": {}
    }
  },
  ...
}
```

## Status: complete
