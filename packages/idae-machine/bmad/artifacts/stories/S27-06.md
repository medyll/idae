# S27-06 — Tests + Playwright _views pilotent colonnes et form

**Sprint:** 27
**Effort:** M
**Priority:** high
**Dependencies:** S27-01 à S27-05

---

## Unit tests

- `server/src/__tests__/getModel.test.ts` (CREATE) — mock appscheme_view query → assert `_views` construit correctement par collection
- `src/lib/main/__tests__/machineScheme.test.ts` — assert `scheme.views` getter retourne shape correcte

## Playwright golden path

1. Full reset via DevResetPanel
2. App charge → sidebar 6 collections
3. Click "vehicle" → Explorer list → **colonnes = _views.list fields** (license_plate, model, brand, year, status)
4. Click record → Explorer card → **CardForm fields = _views.form** (tous les champs dans l'ordre)
5. 0 console errors

## Acceptance

- `pnpm run test` ≥ 439/439
- `pnpm run check` → 0 new errors
- Playwright: colonnes liste correspondent à `_views.list`, form correspond à `_views.form`
- Screenshot final livré
