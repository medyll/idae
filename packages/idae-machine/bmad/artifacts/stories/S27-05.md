# S27-05 — Deploy demo + verify _views en MongoDB et API

**Sprint:** 27
**Effort:** S
**Priority:** high
**Dependencies:** S27-01

---

## Goal

Vérifier bout-en-bout que `deployModel()` + `getModel()` produit des `_views` correctes pour les collections demo.

## Steps

1. DevResetPanel → Full reset (clear + deploy + seed)
2. Vérifier MongoDB: `appscheme_view` a des rows pour vehicle, category, customer
3. Vérifier API: `GET /api/scheme` (ou équivalent) retourne model avec `_views`
4. Log attendu:
   ```
   vehicle._views.list = ['license_plate','model','brand','year','status']
   vehicle._views.form = ['id','license_plate','model','brand','year','categoryId','locationOfficeId','status','mileage','created_at']
   vehicle._views.fk_label = ['license_plate','model']
   ```

## Acceptance

- MongoDB `appscheme_view` non vide après deploy
- `_views` présent dans model retourné par `getModel()`
- Champs `list` correspondent à `template.presentation` de demoScheme
- 0 erreur serveur pendant deploy
