# S25-06 — Tests + Playwright golden path

**Sprint:** 25
**Effort:** M
**Priority:** high
**Dependencies:** S25-01 à S25-05 (tous complets)

---

## Goal

Vérifier absence de régression, golden path Playwright: sidebar → Explorer list → Explorer card.

## Unit tests

- `src/lib/main/__tests__/paneComponents.test.ts` — retirer refs PaneLeft; ajouter Explorer render tests si applicable
- `src/lib/main/__tests__/componentRegistry.test.ts` — assert `'explorer'` enregistré, `'explorer.list'`/`'explorer.table'`/`'explorer.card'`/`'explorer.actions'` absents
- `pnpm run test` → ≥ 439/439

## Playwright golden path

1. App charge → AppShell visible → sidebar affiche 6 collections (vehicle, category, customer, rental, location_office, maintenance)
2. Click "vehicle" → `machine.loadFrame('explorer', 'vehicle', undefined, { mode: 'list' })` → Frame[main] monte Explorer(collection='vehicle', mode='list') → liste records visible
3. Click un record vehicle → Frame[main] met à jour → Explorer(mode='card', collectionId=X) → CardForm affiché
4. 0 erreurs console, 0 erreurs réseau

## Acceptance

- Tests: `pnpm run test` ≥ 439/439
- Check: `pnpm run check` → 0 new errors
- Playwright: 3 étapes golden path passent
- Screenshot final livré
