# Story S9-01 — machine.fetchSchema() + stale-while-revalidate

**Sprint:** 9  
**Goal:** Client-side async schema init from server URL with IDB cache

## Implemented

| File | Action |
|------|--------|
| `src/lib/main/machineSchemaCache.ts` | NEW — IDB read/write for _machine_schema_cache/_schema |
| `src/lib/main/machine.ts` | MOD — fetchSchema(url) method + import machineSchemaCache |

## Behavior

```typescript
const emitter = await machine.fetchSchema('/api/scheme');
// machine already started (from cache or fresh fetch)

emitter.addEventListener('schema:updated', (e) => {
  // New schema available — reload or update UI
});
```

1. If cache exists: `machine.start()` immediately with cached model
2. Background fetch: compare to cache, update IDB + emit `schema:updated` if changed
3. If no cache: await fetch, write cache, start

## Status: complete
