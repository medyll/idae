# S21-04 — `field('image')` extension + `FileMeta.image.focus` (UI-ready)

**Sprint:** 21
**Effort:** M
**Priority:** medium
**Dependencies:** S21-01

---

## Context

Aujourd'hui :
- `field()` builder n'a pas de type `'image'`. Aucune façon de déclarer `cover: field('image', { presets: [...] })` dans un schéma domain.
- `FieldDisplay.svelte` n'a pas de branche image — fallback générique `<input type={htmlInputType}>`.
- `FileMeta.image` (de S20-04) capture `width/height/format/hasAlpha`. **Pas de focus point** — recadrage cover est toujours centré, info perdue si l'utilisateur a un sujet décentré.

Goal :
1. Étendre `field()` builder pour type `'image'` avec options `presets`, `preset` (singulier), `free`, `maxSize`
2. Ajouter `focus?: { x: number; y: number }` (0..1) sur `FileMeta.image` — provisionné pour UI future, **non câblé dans resolver crop ce sprint** (juste persisté + retourné API)
3. Documenter dans `CLAUDE.md` + `conventions.md`

Pas d'`InputImage` Svelte composant ce sprint. Trop large. Story séparée future quand UI focus-pin drag arrive.

## Files to modify / create

| File | Change |
|------|--------|
| `src/lib/main/machine/fieldBuilder.ts` | Ajouter type `'image'` + options `presets`/`preset`/`free`/`maxSize` |
| `src/lib/types/machine-model.ts` | Étendre `MachineFieldDef` avec champs image (rules object) |
| `src/lib/main/machineParserForge.ts` | Parser type `'image'` → IDbForge |
| `server/src/types/imageMeta.ts` | Ajouter `focus?: { x: number; y: number }` à `ImageMeta` |
| `server/src/services/FileService.ts` | Endpoint patch metadata (set focus) — `updateImageFocus(fileId, focus)` |
| `server/src/routes/files.ts` | `PATCH /api/files/:fileId/focus` body `{ x, y }` |
| `src/lib/main/__tests__/fieldBuilder.test.ts` | Tests type image |
| `server/src/__tests__/files.test.ts` | Tests update focus |

---

## 1. Field builder extension

```ts
// src/lib/main/machine/fieldBuilder.ts (extends)
export interface ImageFieldOptions {
	required?: boolean;
	readonly?: boolean;
	presets?:  string[];      // whitelist par code preset (ex: ['thumb', 'banner'])
	preset?:   string;        // mono-preset (raccourci, exclusive avec presets)
	free?:     boolean;       // accepte free-WxH notation (default: false si presets set, true sinon)
	maxSize?:  number;        // octets (default: config server)
	multiple?: boolean;       // futur (multi-images), accepté mais ignoré ce sprint
}

export function field(type: 'image', options?: ImageFieldOptions): MachineFieldDef;
// ... overloads existants
```

**Validation au parse** :
- `preset` ET `presets` simultanés → erreur build (`throw new Error('Use preset OR presets, not both')`)
- `preset` set → `free: false` forcé (mono-preset strict)
- `presets` set sans `free` explicite → `free: false`
- Ni `preset` ni `presets` → `free: true` par défaut (champ libre tous presets DB + free-*)

## 2. MachineFieldDef extension

```diff
// src/lib/types/machine-model.ts
 export interface MachineFieldDef {
   type:      string;
   required?: boolean;
   readonly?: boolean;
+  presets?:  string[];
+  preset?:   string;
+  free?:     boolean;
+  maxSize?:  number;
+  multiple?: boolean;
   // ...autres options existantes
 }
```

## 3. Parser

`machineParserForge.ts` : type `'image'` → forge `htmlInputType = 'file'`, accept `image/*`. Options `presets`/`preset`/`free` passées telles quelles dans IDbForge pour consultation côté UI/API.

```diff
 case 'image': {
   forge.htmlInputType = 'file';
   forge.accept        = 'image/*';
   forge.presets       = def.presets;
   forge.preset        = def.preset;
   forge.free          = def.free ?? (def.presets ? false : true);
   forge.maxSize       = def.maxSize;
   break;
 }
```

## 4. Focus point sur FileMeta.image

```diff
// server/src/types/imageMeta.ts
 export interface ImageMeta {
   width:    number;
   height:   number;
   format:   string;
   hasAlpha: boolean;
+  focus?:   { x: number; y: number };   // 0..1 normalisés sur l'original
 }
```

```ts
// server/src/services/FileService.ts (ajout)
export async function updateImageFocus(
	org: string,
	fileId: string,
	focus: { x: number; y: number }
): Promise<FileMeta | null> {
	if (focus.x < 0 || focus.x > 1 || focus.y < 0 || focus.y > 1) {
		throw new Error('Focus coordinates must be 0..1');
	}
	const coll = await getFileCollection(org);
	const result = await coll.findOneAndUpdate(
		{ fileId, deletedAt: null, image: { $exists: true } },
		{ $set: { 'image.focus': focus } },
		{ returnDocument: 'after' }
	);
	return result as FileMeta | null;
}
```

Endpoint :
```
PATCH /api/files/:fileId/focus    body: { x: 0.42, y: 0.18 }
middleware: requireFileAccess('U')
```

**Important** : resolver crop (S21-02/03) **n'utilise PAS encore** ce focus. Ce sprint = juste persister. Sprint UI future câblera `sharp.extract` ou region-of-interest. Doc dans `conventions.md` comme "réservé pour future feature crop intelligent".

## 5. Documentation

Ajouter dans `CLAUDE.md` section 5 (Field type reference) :

| Type | HTML input | Notes |
|------|-----------|-------|
| `'image'` | file (accept=image/\*) | options : `presets[]`, `preset`, `free`, `maxSize`. Upload via `/api/files/:coll/:id`, accès variant via `/api/files/:fileId/image/:preset` |

Ajouter dans `conventions.md` nouvelle section :
```md
## Image presets

- Entité système : `appimage_preset` (idae-model-core.ts)
- Seedés au boot : thumb, square, small, medium, large, banner, avatar
- Free notation : `free-WxH` / `free-wW` / `free-hH` → auto-créés à la 1ère requête (flag `auto: true`)
- DOS bounds : 16..4096 px
- Champ schema : `field('image', { presets: ['thumb', 'banner'], free: false })`
- Focus point : `FileMeta.image.focus` réservé pour crop intelligent futur (UI focus-pin)
```

---

## Acceptance criteria

1. `field('image', { presets: ['thumb', 'banner'] })` → MachineFieldDef avec `type: 'image', presets: [...], free: false`
2. `field('image', { preset: 'avatar' })` → `preset: 'avatar', free: false`
3. `field('image', { preset: 'a', presets: ['b'] })` → throw au build
4. `field('image')` → `free: true`, presets undefined
5. Parser : forge `htmlInputType: 'file'`, `accept: 'image/*'`, options propagées
6. `PATCH /api/files/:id/focus` body `{ x: 0.4, y: 0.2 }` → metadata.image.focus mis à jour
7. `PATCH .../focus` body `{ x: -1, y: 0 }` → 400 ou throw
8. `PATCH .../focus` sur file non-image → 404
9. `PATCH .../focus` sans `requireFileAccess('U')` → 403
10. `GET /api/files/:id/meta` post-update → contient `image.focus`
11. Doc `CLAUDE.md` + `conventions.md` mises à jour
12. Suite no regression

## Tests

`fieldBuilder.test.ts` :
- field('image', presets) → MachineFieldDef correct
- field('image', preset) → correct
- field('image', preset+presets) → throw
- field('image') → free=true
- parser image → forge correct (htmlInputType, accept, options)

`files.test.ts` (ajouts) :
- updateImageFocus valid → meta.image.focus set
- updateImageFocus invalid coords → throw
- updateImageFocus sur non-image → null
- PATCH endpoint smoke + permission

## Definition of done

- [ ] `field('image', ...)` opérationnel + parser câblé
- [ ] `MachineFieldDef` étendu (presets/preset/free/maxSize)
- [ ] `ImageMeta.focus?` ajouté
- [ ] `FileService.updateImageFocus` + endpoint PATCH `/focus`
- [ ] Tests fieldBuilder (≥5) + tests files focus (≥4) verts
- [ ] Doc `CLAUDE.md` + `conventions.md`
- [ ] Suite complète green
