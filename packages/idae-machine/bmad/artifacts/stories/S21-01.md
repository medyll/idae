# S21-01 — `appimage_preset` core entity + seed defaults + registry cache

**Sprint:** 21
**Effort:** M
**Priority:** high
**Dependencies:** — (utilise S20-01 hooks pour invalidation cache)

---

## Context

Aujourd'hui, les variants d'images sont une const TypeScript dans `server/src/services/ImageService.ts:18-23` :

```ts
const VARIANTS = {
	thumb:  { w: 150,  h: 150, fit: 'cover' },
	small:  { w: 480,         fit: 'inside' },
	medium: { w: 1024,        fit: 'inside' },
	large:  { w: 2048,        fit: 'inside' },
};
```

Rigide, non éditable runtime, non multi-tenant.

Goal : transformer cette config en **entité système `appimage_preset`** dans `idae-model-core.ts`, seeder defaults au boot, exposer un registry mémoire avec invalidation hook-driven.

## Files to modify / create

| File | Change |
|------|--------|
| `src/lib/types/idae-model-core.ts` | Ajouter collection `appimage_preset` dans `appModelDeclaration.collections` |
| `server/src/bootstrap/seedImagePresets.ts` | **NEW** — seed defaults à la première connexion DB |
| `server/src/bootstrap/seed.ts` | Câbler `seedImagePresets` après `seedUsers` |
| `server/src/services/ImagePresetRegistry.ts` | **NEW** — cache mémoire + load + invalidation |
| `server/src/hooks/builtins.ts` | Hook `post:create/update/delete` sur `appimage_preset` → invalidate cache |
| `server/src/__tests__/imagePresetRegistry.test.ts` | **NEW** tests |
| `server/src/__tests__/setup.ts` | Aucun changement (seed s'auto-applique au boot) |

---

## 1. Entité `appimage_preset` dans `idae-model-core.ts`

À insérer dans `appModelDeclaration.collections`, alphabétiquement après `appfile` futur (ou en fin) :

```ts
appimage_preset: {
	base:   'machine_app',
	rights: { ops: ['R', 'L', 'C', 'U', 'D'], default: ['R', 'L'] },
	fields: {
		id:      { required: true,  readonly: true  },
		code:    { required: true,  readonly: false },  // 'thumb' | 'free-125x88' | ...
		name:    { required: true,  readonly: false },  // libellé humain
		color:   { required: false, readonly: false },
		icon:    { required: false, readonly: false },
		order:   { required: false, readonly: false },
		width:   { required: false, readonly: false },  // pixels
		height:  { required: false, readonly: false },  // pixels
		fit:     { required: false, readonly: false },  // 'cover'|'inside'|'contain'|'fill'
		format:  { required: false, readonly: false },  // 'jpeg'|'webp'|'png'|'auto'
		quality: { required: false, readonly: false },  // 1..100
		auto:    { required: false, readonly: true  },  // true = auto-créé via free-notation
		scope:   { required: false, readonly: false },  // 'global' uniquement ce sprint
	},
	fks: {},
	template: {
		presentation: 'name code',
	},
},
```

`color`/`icon`/`order` requis par convention `appscheme_base` mais marqués `required: false` ici (image presets pas affichés dans la nav par défaut). Engine model gardera defaults vides.

---

## 2. Seed defaults (`seedImagePresets.ts`)

```ts
// server/src/bootstrap/seedImagePresets.ts
import { getConn } from '../middleware/dbRouter.js';
import { config } from '../config.js';
import { logger } from '../utils/logger.js';

interface PresetSeed {
	code:    string;
	name:    string;
	width?:  number;
	height?: number;
	fit:     'cover' | 'inside' | 'contain' | 'fill';
	format:  'jpeg' | 'webp' | 'png' | 'auto';
	quality: number;
}

const DEFAULT_PRESETS: PresetSeed[] = [
	{ code: 'thumb',  name: 'Vignette',   width: 150,  height: 150, fit: 'cover',  format: 'auto', quality: 82 },
	{ code: 'square', name: 'Carré',      width: 120,  height: 120, fit: 'cover',  format: 'auto', quality: 82 },
	{ code: 'small',  name: 'Petit',      width: 480,               fit: 'inside', format: 'auto', quality: 82 },
	{ code: 'medium', name: 'Moyen',      width: 1024,              fit: 'inside', format: 'auto', quality: 82 },
	{ code: 'large',  name: 'Grand',      width: 2048,              fit: 'inside', format: 'auto', quality: 82 },
	{ code: 'banner', name: 'Bannière',   width: 1920, height: 480, fit: 'cover',  format: 'auto', quality: 82 },
	{ code: 'avatar', name: 'Avatar',     width: 256,  height: 256, fit: 'cover',  format: 'auto', quality: 82 },
];

export async function seedImagePresets(): Promise<void> {
	const conn = await getConn(`${config.org}_machine_app`);
	const coll = conn.collection('appimage_preset');

	for (const seed of DEFAULT_PRESETS) {
		const existing = await coll.findOne({ code: seed.code });
		if (existing) continue;
		await coll.insertOne({ ...seed, auto: false, scope: 'global' });
	}

	logger.info(`Seeded ${DEFAULT_PRESETS.length} image presets in ${config.org}_machine_app.appimage_preset`);
}
```

Câblage dans `bootstrap/seed.ts` :
```diff
 import { seedUsers } from './seedUsers.js';
+import { seedImagePresets } from './seedImagePresets.js';
 // ...
 await seedUsers();
+await seedImagePresets();
```

**Idempotent** : skip si `code` existant. Pas d'update — si un admin a customisé `thumb`, on respecte.

---

## 3. Registry cache

```ts
// server/src/services/ImagePresetRegistry.ts
import { getConn } from '../middleware/dbRouter.js';
import { config } from '../config.js';
import { logger } from '../utils/logger.js';

export interface ImagePreset {
	code:    string;
	name:    string;
	width?:  number;
	height?: number;
	fit:     'cover' | 'inside' | 'contain' | 'fill';
	format:  'jpeg' | 'webp' | 'png' | 'auto';
	quality: number;
	auto:    boolean;
	scope:   string;
}

const cache = new Map<string, ImagePreset>();
let loaded = false;

async function getCollection() {
	const conn = await getConn(`${config.org}_machine_app`);
	return conn.collection('appimage_preset');
}

export async function loadAll(): Promise<void> {
	const coll = await getCollection();
	const rows = await coll.find({}).toArray();
	cache.clear();
	for (const row of rows) {
		cache.set(row.code as string, row as unknown as ImagePreset);
	}
	loaded = true;
	logger.info(`ImagePresetRegistry loaded: ${cache.size} presets`);
}

export async function get(code: string): Promise<ImagePreset | null> {
	if (!loaded) await loadAll();
	return cache.get(code) ?? null;
}

export async function getAll(): Promise<ImagePreset[]> {
	if (!loaded) await loadAll();
	return Array.from(cache.values());
}

export async function insert(preset: ImagePreset): Promise<void> {
	const coll = await getCollection();
	await coll.insertOne(preset);
	cache.set(preset.code, preset);
}

export function invalidate(): void {
	cache.clear();
	loaded = false;
}

export function _testReset(): void {
	cache.clear();
	loaded = false;
}
```

---

## 4. Hook invalidation

Dans `server/src/hooks/builtins.ts`, ajouter dans `registerBuiltinHooks()` :

```ts
import * as ImagePresetRegistry from '../services/ImagePresetRegistry.js';

registerHook('post:create', (ctx) => {
	if (ctx.collection === 'appimage_preset') ImagePresetRegistry.invalidate();
}, { priority: 10 });

registerHook('post:update', (ctx) => {
	if (ctx.collection === 'appimage_preset') ImagePresetRegistry.invalidate();
}, { priority: 10 });

registerHook('post:delete', (ctx) => {
	if (ctx.collection === 'appimage_preset') ImagePresetRegistry.invalidate();
}, { priority: 10 });
```

Priority 10 = très tôt, avant audit/broadcast.

---

## Acceptance criteria

1. `appimage_preset` apparaît dans `appModelDeclaration.collections`, structure conforme convention (`code`, `name`, ...)
2. `seedImagePresets()` insère 7 presets default si absents, skip si présents (idempotent)
3. Re-run seed sur DB déjà semée → aucun doublon, aucun warning
4. `ImagePresetRegistry.get('thumb')` retourne `{ code: 'thumb', name: 'Vignette', width: 150, height: 150, fit: 'cover', ... }`
5. `ImagePresetRegistry.get('inexistant')` → `null`
6. `ImagePresetRegistry.getAll()` → 7 entries après seed
7. CRUD via `POST /api/data/appimage_preset` (hooks S20-01) → `invalidate()` appelé, prochain `get` recharge depuis DB
8. Permissions: RBAC `appimage_preset` opérationnel (R/L par défaut, C/U/D admin uniquement) — réutilise pattern `requireDroit`
9. Tests verts, suite complète no regression

## Tests (`imagePresetRegistry.test.ts`)

- seed → 7 presets en DB, codes corrects
- seed × 2 → toujours 7 (idempotent)
- get('thumb') → preset trouvé
- get('inexistant') → null
- getAll() → 7
- insert(custom) → cache contient custom
- invalidate() → reload sur prochain get
- hook `post:update` sur `appimage_preset` → cache invalidé (utiliser `dispatch` du HooksRegistry directement dans test)

## Definition of done

- [ ] Entité `appimage_preset` dans `idae-model-core.ts`
- [ ] `seedImagePresets.ts` créé + câblé dans `seed.ts`
- [ ] `ImagePresetRegistry.ts` créé (get/getAll/insert/invalidate/loadAll)
- [ ] Hooks invalidation câblés dans `builtins.ts`
- [ ] `imagePresetRegistry.test.ts` ≥ 8 tests verts
- [ ] Suite complète no regression
- [ ] Doc convention dans `conventions.md` section nouvelle "Image presets"
