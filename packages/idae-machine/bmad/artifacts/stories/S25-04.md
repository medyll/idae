# S25-04 — Update +layout.svelte — use AppShell, remove PaneLeft

**Sprint:** 25
**Effort:** S
**Priority:** high
**Dependencies:** S25-01 (AppShell opérationnel)

---

## Goal

Simplifier `+layout.svelte` au minimum: init machine + `<AppShell />`. Tout le reste délégué à AppShell.

## Files

| File | Change |
|------|--------|
| `src/routes/+layout.svelte` | Simplification — remplacer markup inline par `<AppShell />` |

## Before (schéma)

```svelte
<script>
  import PaneLeft, Frame, DevResetPanel...
  machine.init(...); machine.start(); machine.initRouter(...)
  // seedIfEmpty, sidebarCollapsed, devPanelOpen, handleCollectionSelect...
</script>
<div class="app-shell">
  <header>...</header>
  <aside><PaneLeft .../></aside>
  <main><Frame id="main" /></main>
</div>
```

## After

```svelte
<script lang="ts">
  import { machine } from '$lib/main/machine.js';
  import { demoScheme, demoSeed } from '$lib/demo/demoScheme.js';
  import AppShell from '$lib/shell/layout/AppShell.svelte';
  import type { LayoutProps } from './$types.js';
  import '../app.css';

  let { children }: LayoutProps = $props();

  machine.init({ org: 'demo', domain: 'machine', dbName: 'testdb', version: 1, model: demoScheme });
  machine.start();
  machine.initRouter({ baseUrl: '/', authEnabled: false });

  async function seedIfEmpty() {
    // appscheme seed + demo data (inchangé)
  }

  $effect(() => { seedIfEmpty(); });
</script>

<AppShell />
{@render children()}
```

## Acceptance

- Aucun import de PaneLeft, PaneCollectionGroup, Frame, DevResetPanel au niveau layout (tout dans AppShell)
- `seedIfEmpty` reste dans layout (logique demo data)
- `machine.init/start/initRouter` restent dans layout (bootstrap)
- CSS app-shell dans AppShell.svelte, pas dans +layout.svelte
- App fonctionne: sidebar visible, Frame[main] présent
