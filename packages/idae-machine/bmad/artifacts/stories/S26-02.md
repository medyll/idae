# S26-02 — Rename _prefs/_activity/_history → appuser_prefs/appuser_activity/appuser_history dans idae-model-core.ts

**Sprint:** 26
**Effort:** S
**Priority:** high
**Dependencies:** S26-01 (décision résolue: garder appuser_audit séparé)

---

## Context

`_prefs`, `_activity`, `_history` cassent la convention `app{domain}_*`. Toutes trois sont des données per-user → domaine `appuser_*`. La décision S26-01 confirme: `appuser_audit` (security log readonly) reste séparé de `appuser_activity` (user event log rw).

## Files

| File | Change |
|------|--------|
| `src/lib/types/idae-model-core.ts` | Rename 3 clés + corriger base |

## Renames

| Avant | Après | base avant | base après |
|-------|-------|-----------|-----------|
| `_prefs` | `appuser_prefs` | vérifier | `machine_user` |
| `_activity` | `appuser_activity` | vérifier | `machine_user` |
| `_history` | `appuser_history` | vérifier | `machine_user` |

## Acceptance

- Les 3 clés renommées dans `collections` de `appModelDeclaration`
- `base: 'machine_user'` sur les 3 (données per-user, pas schema/config)
- `keyPath: '++id'` et tous autres champs préservés à l'identique
- Aucune autre logique changée dans ce fichier
