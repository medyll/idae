# S17-02 — Wire SchemeValidator dans createRecord/updateRecord

**Sprint:** 17  
**Effort:** S  
**Priority:** High  
**Deps:** S17-01

## What to do

`server/src/routes/data.ts` — ajouter SchemeValidator avant domain actions dans create et update.

### createRecord — ordre final

```ts
// 1. Generic schema validation
const schemeResult = await validateAgainstScheme(table, req.body);
if (!schemeResult.valid) {
  res.status(422).json({ error: 'Validation failed', errors: schemeResult.errors });
  return;
}

// 2. Domain custom validation (extension/override)
const domainActions = getDomainActions(table);
if (domainActions?.validate) {
  const result = domainActions.validate(req.body, table);
  if (!result.valid) {
    res.status(422).json({ error: 'Validation failed', errors: result.errors });
    return;
  }
}

// 3. Persist
const record = await Model.create(req.body);
```

### updateRecord — même ordre

Idem — SchemeValidator avant domain validate(), avant `findByIdAndUpdate`.

### Import à ajouter

```ts
import { validateAgainstScheme } from '../validation/SchemeValidator.js';
```

### Invalidation cache au bootstrap

Dans `server/src/routes/bootstrap.ts` ou `deployModel.ts`, après déploiement :
```ts
import { invalidateSchemeCache } from '../validation/SchemeValidator.js';
invalidateSchemeCache(); // reset après seed
```

## Acceptance criteria

- [ ] `POST /api/data/vehicule` avec champ required manquant → 422 + errors
- [ ] `POST /api/data/vehicule` valide → 201
- [ ] `POST /api/data/collection_inconnue` → pas de 422 (graceful skip), 500 si collection pas dans appscheme (c'est dbRouter qui throw)
- [ ] Domain actions.validate() appelé APRÈS SchemeValidator
- [ ] Erreurs des deux layers mergées si besoin (ou retour au premier échec — retour au premier échec est OK)
- [ ] invalidateSchemeCache() appelé après bootstrap/deployModel
