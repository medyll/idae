# S26-05 — Tests + check + grep final zéro référence _*

**Sprint:** 26
**Effort:** S
**Priority:** high
**Dependencies:** S26-02, S26-03, S26-04

---

## Goal

Vérification finale: zéro référence aux anciens noms, tests verts, check propre.

## Acceptance

- `grep -rn "'_prefs'\|'_activity'\|'_history'" src/ server/` → 0 résultats
- `pnpm run test` → ≥ 439/439
- `pnpm run check` → 0 new errors
- Convention dans bmad/conventions.md: pattern `app{domain}_*` obligatoire pour toute nouvelle collection système, pas de `_*` shorthand
