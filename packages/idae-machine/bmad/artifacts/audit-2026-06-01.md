# Audit — idae-machine — 2026-06-01

> Reviewer role (bmad-audit). Scope: full codebase + this session's 4 commits.

## Health score: 8.5 / 10

Strong client health (0 type errors, full client suite green, lint clean, no
security smells). Single deduction: 23 pre-existing server integration test
failures (Mongo-dependent / signature drift), unrelated to this session's work.

---

## Tests

| Suite | Result |
|-------|--------|
| Client `pnpm check` | **0 errors, 0 warnings** (1301 files) |
| Client `pnpm test` | **586 / 586 pass** (52 files) |
| Server `pnpm test` | 145 pass, **23 fail**, 13 skipped (7 files fail) |

**Server failures — pre-existing, NOT introduced this session.** Baseline before
work = 28 failures; now 23. The −5 delta is exactly the `seedBusinessData.test.ts`
file, which this session repaired (stale 2-arg signature → current opts signature
+ `clearFirst` coverage). The remaining 23 are Mongo-integration / signature-drift
failures across 7 files — flagged below as an action item, out of session scope.

---

## Security scan

- `eval` / `new Function` / `innerHTML` / `dangerouslySetInnerHTML`: **none** in src.
- This session adds no network sinks, no auth changes. `--reseed` is a destructive
  seed flag (deleteMany) but gated behind an explicit CLI arg, default off. OK.

## Code quality / lint

- Prettier drift: **0 files** (improved from ~270 in prior audits — formatting clean).
- Session diff: **27 files, +107 / −240 (net −133)** — removal-heavy, healthy.
  Dead code eliminated (DataFkValue + duplicate FK branch), no new abstraction.

---

## This session — change review

| Commit | Assessment |
|--------|-----------|
| `fix(seed)` FK columns invisible — reseed | **Correct root-cause fix.** Identified stale Mongo data (`categoryId` numeric vs model `category` code) persisting through idempotent seed. `clearFirst`/`--reseed` is the right surgical lever; default-off preserves idempotence. Verified live: FK now resolve ("Compact compact"). |
| `refactor(ui)` remove DataFkValue | **Good consolidation.** Genuine duplicate of DataField show-mode FK path. Label resolution centralized in DataField. `view='fk'` branch dead code (redundant filter) removed. |
| `refactor(types)` rename by role | **Reduces real LLM/maintainer confusion.** `idae-model-core`→`engineMetaSeed` (role-accurate), client `demoScheme`→`__fixtures__/demoModel` (test-only), dead `bootstrap/demoScheme` deleted, value removed from types barrel. Only one `demoScheme` name remains (server seed) — collision gone. |
| `chore` VIEWS.md typo + --host | Trivial, correct. |

No correctness or security regressions found in the session diff.

---

## Naming & structure findings

- ✅ Session improves naming-by-role (the rename commit directly addresses the
  `[Category][SemanticIntent]` convention gap that existed for these files).
- ⚠ **Pre-existing**, unchanged this session (from backlog, still open):
  - BL-02: `data-ui/` imports `sortItems/groupItems` from `shell/frame/` — upward
    dependency, wrong direction. Move to `data-ui/utils/`.
  - BL-06: `data-collectionId` camelCase → HTML lowercases to `data-collectionid`,
    `dataset.collectionId` breaks. Rename to kebab `data-collection-id`.

---

## Prioritized action items

1. **(Important)** Triage the 23 server integration failures (7 files). Likely
   Mongo-connection / stale signatures like the one fixed in `seedBusinessData.test.ts`.
   Pattern: tests written against an older API the impl outgrew. Worth a dedicated
   sprint story — they erode the server suite's signal.
2. **(Minor)** BL-02 upward dependency `data-ui/ → shell/frame/`.
3. **(Minor)** BL-06 `data-collectionId` kebab rename.

No release blockers. Phase stays `release`.
