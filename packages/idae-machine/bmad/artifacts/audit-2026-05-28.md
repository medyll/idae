# Audit Report — idae-machine
**Date:** 2026-05-28  
**Auditor:** Reviewer (bmad-audit)  
**Scope:** Full codebase since last audit (2026-05-27)

---

## Summary

| Metric | Value | Δ since last audit |
|--------|-------|-------------------|
| Health score | **8.5 / 10** | — |
| Tests (unit) | **541 / 541 pass** | −6 tests (consolidation?) |
| svelte-check | **0 errors, 0 warnings** | ✅ −2 warnings |
| Lint drift | **272 files** | 🔺 +2 files |
| Flaky tests | **1 detected** | 🔺 new |

---

## Findings

### 🔴 Important — Flaky Test
- **File:** `src/lib/main/__tests__/machineClient.test.ts`
- **Test:** `boot resolves without error on subsequent calls`
- **Symptom:** Intermittent 5000ms timeout when running the full suite. Passes consistently in isolation.
- **Likely cause:** Test pollution from shared global state (IDB cache, `fetch` stub, or `Machine` singleton leakage across test files).
- **Action:** Add explicit cache invalidation / IDB cleanup in `beforeEach`, or extend timeout with a TODO to fix root cause.

### 🟡 Minor — Format Drift
- 272 files need Prettier formatting (+2 since last audit).
- No parse errors (the 2 from `status.yaml` and `S25-04.md` remain fixed).

### 🟢 Improved
- svelte-check: previously 0 errors + 2 warnings (unused CSS); now **0 errors, 0 warnings**.

---

## Code Changes Reviewed (since 2026-05-27)

| File | Change | Assessment |
|------|--------|------------|
| `machineClient.test.ts` | Added `ok: true` to mock fetch; refactored subsequent-boot test to use new `Machine` instance | Intent is good, but may have exposed flakiness |
| `frameIntegration.test.ts` | Renamed test descriptions (`frameManager` → `MachineFrameManager`) | Minor, improves clarity |
| `machine.ts` | Comment updated (`seedIfEmpty` → `seed(..., { onlyIfEmpty: true })`) | Accurate, reflects deprecation |
| `machineSchemaCache.ts` | Comment updated to mention `boot()` instead of `fetchSchema()` | Accurate |
| `machineSchemaLoader.ts`, `machineModelBuilder.ts`, `MachineRights.ts`, `idae-model-core.ts` | Trivial 1–2 line changes | No concerns |
| `e2e/rbac-matrix.spec.ts` | Minor tweak (2 lines) | Deferred to Playwright run |
| `machineLoadFrame.test.ts` | Minor tweak (2 lines) | No concerns |

---

## Security Scan
- No `eval` / `innerHTML` in source.
- No new dependencies introduced.
- No auth or injection risks in changed code.

---

## Action Items (prioritized)

1. **Fix flaky test** — `machineClient.test.ts` subsequent-boot timeout. Investigate shared IDB / cache state between test files.
2. **Run `pnpm format`** — 272 files drift.
3. **Address remaining backlog items** — `BACK-05` (Playwright RBAC) still high priority.
4. **DataList pageSize reactivity** — flagged in previous audit, still pending.
5. **machine.ts ↔ MachineAction.ts circular dep** — flagged in previous audit, still pending.

---

## Verdict

No blockers for release. One flaky test needs attention before it becomes a CI reliability issue.
