/**
 * BOOTSTRAP ONLY — dev/test/seed use only.
 * Never import this in production routes or +page.svelte.
 * Run seedSchemeFromModel(demoScheme, opts) once → MongoDB appscheme_*.
 */
export { demoScheme, demoSeed } from '$lib/demo/demoScheme.js';
