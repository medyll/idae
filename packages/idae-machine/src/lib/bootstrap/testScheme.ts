/**
 * BOOTSTRAP ONLY — dev/test/seed use only.
 * Never import this in production routes or +page.svelte.
 * Run seedSchemeFromModel(testScheme, opts) once → MongoDB appscheme_*.
 */
export { testScheme, demoSeed } from '$lib/demo/testScheme.js';
