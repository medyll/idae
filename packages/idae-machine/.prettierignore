# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock

# Ignore Svelte files for Prettier due to plugin compatibility in this workspace
# Prettier will still check other file types.
**/*.svelte
**/*.svelte.ts
node_modules/
dist/

# BMAD is skill-owned tracked state: bmad/status.yaml indexes the artifacts/ tree,
# so the docs must NOT be relocated (the bmad-method skill's references would break).
# Ignore the whole tree for formatting noise. status.yaml also has folded scalars Prettier can't parse.
bmad/
