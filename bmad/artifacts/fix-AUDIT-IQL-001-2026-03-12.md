# Fix: AUDIT-IQL-001 — Remove @ts-ignore from idae-idbql

Date: 2026-03-12

Summary:
- Removed a single `@ts-ignore` usage in `packages/idae-idbql/src/lib/state/idbqlEvent.svelte.ts` and cleaned up the surrounding comments.
- Verified no remaining `@ts-ignore` occurrences within the `idae-idbql` package.

Details:
- Change: removed the unused `@ts-expect-error` directive and the `eslint-disable` comment, leaving the `$state` assignment intact.
- Rationale: suppressing TypeScript errors with `@ts-ignore` masks real issues. Where suppression was unavoidable, prefer `@ts-expect-error` with a justification. In this case the directive was unused so removal is appropriate.

Verification:
- Ran `pnpm --filter @medyll/idae-idbql run check` (svelte-check). The command completed but reported 47 unrelated TypeScript diagnostics across several files. These pre-existing issues are out of scope for this small audit fix.
- Confirmed no `@ts-ignore` occurrences remain in `packages/idae-idbql`.

Follow-up:
- Recommend a dedicated story to address the remaining TypeScript diagnostics across `idae-idbql` (type-safety improvements and test fixes).

Files changed:
- packages/idae-idbql/src/lib/state/idbqlEvent.svelte.ts

Status:
- AUDIT-IQL-001 marked done in the tracker.

Generated-by: bmad-master next --auto
