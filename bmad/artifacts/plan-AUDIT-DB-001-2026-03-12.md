# Plan: AUDIT-DB-001 — Add ChromaDB test suite

Date: 2026-03-12

Goal: Provide integration tests for the ChromaDB adapter (packages/idae-db) to ensure basic CRUD, query, and adapter behavior under realistic conditions.

Approach:
1. Create an integration test folder: tests/integration/chroma.integration.test.ts (scaffold created by the orchestrator).
2. Two test strategies (choose one):
   - Local Dev Server: Run a local ChromaDB instance (Docker or local dev server) during tests.
   - Mocking: Mock the Chroma client for CI-friendly, faster tests.

Minimum test cases:
- Adapter initialization and connection management
- Create collection and insert documents
- Basic retrieval by id and metadata
- Unsupported operations (updateWhere/deleteWhere) throw expected errors
- Singleton behavior across multiple connections

CI and commands:
- Add npm script: `test:integration` to run Vitest only on the `tests/integration` folder.
- Optionally add a GitHub Actions job `integration-chromadb` that starts a ChromaDB service or sets up a mock environment.

Next steps for implementer:
1. Implement tests in `packages/idae-db/tests/integration/chroma.integration.test.ts` (scaffold created).
2. Choose strategy (local server vs mock) and document in the test file.
3. Run locally with: `pnpm --filter @medyll/idae-db run test -- --run tests/integration/chroma.integration.test.ts`
4. Add CI job if stable.

Owner: package maintainer (suggest: @medyll/idae-db maintainers)

Generated-by: bmad-master next --auto
