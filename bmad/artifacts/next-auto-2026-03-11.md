# BMAD Next (auto) — 2026-03-11

Selected next tasks (auto-assigned):

- AUDIT-IQL-001: Remove `@ts-ignore` from idae-idbql
  Rationale: improve type-safety and enable stricter TypeScript checks across the package.
  Suggested steps:
    1. Search for `@ts-ignore` in `packages/*/idae-idbql` and identify each occurrence.
    2. Replace with proper type annotations or a justified `@ts-expect-error` where unavoidable.
    3. Run `pnpm --filter idae-idbql test` and fix any resulting type/test failures.
    4. Add a small unit test that reproduces the previously suppressed issue where applicable.

- AUDIT-DB-001: Add ChromaDB test suite (idae-db)
  Rationale: ChromaDB adapter lacks integration tests; coverage needed to ensure reliability.
  Suggested steps:
    1. Create an integration test project under `packages/idae-db/test/integration/chromadb`.
    2. Mock or spin up a local ChromaDB instance/fixture and run basic insert/query flows.
    3. Add CI job or extend existing test matrix to include the ChromaDB checks.

Notes:
- These two items were chosen from `bmad/status.yaml` backlog due to high priority and immediate impact on production quality.
- Owners: TBD. Assign to package maintainers or create dev stories in the tracker.

Generated-by: bmad-master next --auto
